[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --disable-warnings

markers =
    unit: Tests unitarios de lógica de negocio
    integration: Tests de integración entre componentes
    ui: Tests de interfaz de usuario
    e2e: Tests end-to-end de flujos completos
    slow: Tests que toman más tiempo (>5 segundos)
    performance: Tests de rendimiento y escalabilidad
    security: Tests de seguridad y vulnerabilidades
    skip_ci: Tests que se saltan en integración continua
