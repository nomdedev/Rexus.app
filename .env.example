# .env.example - variables de entorno necesarias para Rexus.app
# Copia este archivo a `.env` y ajusta los valores locales.

DATABASE_URL=sqlite:///rexus.db
SECRET_KEY=changeme
ENV=development
DEBUG=true
# Ejemplo de variables de entorno para Rexus.app
# Copia este archivo a `.env` y completa valores sensibles antes de ejecutar

# Entorno: development | production
ENV=development

# Database URL (ejemplo sqlite fallback)
# Para producción usar: mssql+pyodbc://user:pass@dsn
DATABASE_URL=sqlite:///./data/rexus.db

# Secret key para sesiones y tokens (no dejar en repos)
SECRET_KEY=replace_this_with_a_secure_value

# Debug flag
DEBUG=True

# Email settings (opcional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Example environment variables for Rexus.app
# Copy to .env and fill values for your environment.

DB_DSN=REXUS_DB_DSN
DB_USER=your_db_user
DB_PASSWORD=your_db_password
SECRET_KEY=change-me
ENV=development
